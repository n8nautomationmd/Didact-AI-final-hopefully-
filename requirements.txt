# Cloud-safe default requirements for Streamlit deployment
# Use requirements-full.txt when you need the full TensorFlow-enabled setup
streamlit>=1.35.0
pandas>=2.2.0
numpy>=2.0.0
openpyxl>=3.1.1

# Machine Learning
scikit-learn>=1.3.2
joblib>=1.3.2

# For TensorFlow, install requirements-full.txt instead.
# For lightweight local dev with TensorFlow CPU support, you can also install
# the CPU-only TensorFlow build here.
tensorflow-cpu>=2.16.1
