# Lightweight requirements for Streamlit Cloud deployment
# This is an alias for the default requirements.txt used by deployment
# Use requirements-full.txt for the TensorFlow-enabled environment

streamlit>=1.35.0
pandas>=2.2.0
numpy>=2.0.0
openpyxl>=3.1.1
scikit-learn>=1.3.2
joblib>=1.3.2

# Note: tensorflow-cpu is optional for this app
# Without it, you get rule-based feedback instead of neural predictions
# Both work equally well for the tutor experience
