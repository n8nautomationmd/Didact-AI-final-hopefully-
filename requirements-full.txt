# Core dependencies
streamlit>=1.35.0
pandas>=2.2.0
numpy>=2.0.0
openpyxl>=3.1.1

# Machine Learning
scikit-learn>=1.3.2
joblib>=1.3.2

# Neural network (optional, lightweight for cloud)
tensorflow-cpu>=2.16.1

# EDA visualisation (only needed to regenerate assets via `python -m src.eda`;
# the app renders pre-generated PNGs, so these are not required at run time)
matplotlib>=3.7
seaborn>=0.12
wordcloud>=1.9

# Optional XGBoost comparison in src/train_models.py (skipped gracefully if absent)
xgboost>=2.0

# NOTE: the REST API layer (FastAPI + uvicorn) lives in requirements-api.txt
